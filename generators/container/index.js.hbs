/*
 *
 * {{properCase name }}
 *
 */

import React, { Component } from 'react';
// import PropTypes from 'prop-types';
import { connect } from 'react-redux';
// import { branch } from 'recompose';
import { createStructuredSelector } from 'reselect';
import {{ absulutePath true toWrap}} from '{{ absulutePath false toWrap}}';


class {{properCase name}} extends Component {

  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return <{{ absulutePath true toWrap}} />;
  }
}

{{properCase name}}.propTypes = {
};

{{properCase name}}.defaultProps = {
};

{{properCase name}}.contextTypes = {
};


const mapStateToProps = createStructuredSelector({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)({{properCase name}});
