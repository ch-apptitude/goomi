/*
 * UserProfile Messages
 *
 * This contains all the text for the UserProfile component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'user.UserProfile.header',
    defaultMessage: 'UserProfile',
  },
});
