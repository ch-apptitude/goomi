/*
import { takeEvery } from 'redux-saga';
import { LOCATION_CHANGE } from 'react-router-redux';

export function* onRouteChange() {
  yield takeEvery(LOCATION_CHANGE, function* locationChangeHandler() {});
}
*/
