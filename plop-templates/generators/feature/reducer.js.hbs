/*
 *
 * {{ properCase name }} reducer
 *
 */

import { fromJS } from 'immutable';
import { DEFAULT_ACTION, API_POST } from './actions';

const initialState = fromJS({});

const {{ camelCase name }}Reducer = (state = initialState, action) => {
  switch (action.type) {
    case DEFAULT_ACTION:
      return state;
    case API_POST.REQUEST:
      return state;
    case API_POST.SUCCESS:
      return state;
    case API_POST.FAILURE:
      return state;
    default:
      return state;
  }
}

export default {{ camelCase name }}Reducer;
