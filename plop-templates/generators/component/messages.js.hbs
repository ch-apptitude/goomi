/*
 * {{ properCase name }} Messages
 *
 * This contains all the text for the {{ properCase name }} component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: '{{ toMessageId subFolder }}.{{ properCase name }}.header',
    defaultMessage: '{{ properCase name}}',
  },{{#if isForm}}
  submit: {
    id: '{{ toMessageId subFolder }}.{{ properCase name }}.submit',
    defaultMessage: 'Submit',
  },{{/if}}
});
